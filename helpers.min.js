const createVenueHTML=(e,n,a)=>
//n.name
`<h2>${e}</h2>\n 

<img class="venueimage" src="${a}"/>\n 

<h3>Location:</h3>\n  
<p>${n.address}</p>\n  
<p>${n.city}</p>\n  
<p>${n.country}</p>`,
//n.location

/*Working on this still. This functions populates the tiles with 6 days forecast*/
createWeatherHTML=n=>`<h2>${n.date.maxtemp} ℃</h2>\n    
<img src="https://${n.hourly.weather_icons}" class="weathericon" />\n    
<h2>${weekDays[new Date(n.date).getDay()]}</h2>`;